name: CI

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  placeholder:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Verify repository
        run: echo "DevFlow CI initialized successfully"
